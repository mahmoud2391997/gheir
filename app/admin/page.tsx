import AdminPanel from "@/src/components/AdminPanel";

export const metadata = { title: "Admin — GHEIR", robots: { index: false, follow: false } };

export default function Page() {
  return <AdminPanel />;
}
